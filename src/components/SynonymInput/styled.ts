import SynonymInput from "./SynonymInput";
import styled from "styled-components";

export default styled(SynonymInput)`
    .info-icon {
        display: flex;
        opacity: .5;
    }
    
    .header {
        display: flex;
        align-items: center;
        
        h2 {
            margin-right: 10px;
        }
        
        color: #4F4F4F;
        margin-bottom: 14px;
    }
`;
